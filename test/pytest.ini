;; pytest configuration for project scope and individual tests under __main__
[pytest]
addopts = -rsxP -l --tb=short --ignore-glob=*/json_samples/*
filterwarnings = ignore::pytest.PytestAssertRewriteWarning
