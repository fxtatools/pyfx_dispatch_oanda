# NOTE: This file is auto generated by OpenAPI Generator.
# URL: https://openapi-generator.tech
#
# ref: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-python

name: pyfx.dispatch.oanda Python package

on: [push, pull_request]

jobs:
  build:

    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python-version: ["3.8", "3.9", "3.10", "3.11"]

    steps:
      - uses: actions/checkout@v3
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}
      - name: Create virtual environment. Install project and dependencies
        run: |
          python -m venv env
          . env/bin/activate; env/bin/pip install --upgrade pip wheel setuptools
          . env/bin/activate; env/bin/pip install -e ".[dev]"
      - name: Lint with flake8
        run: |
          # stop the build if there are Python syntax errors or undefined names
          . env/bin/activate; env/bin/flake8 --extend-exclude src/pyfx/dispatch/oanda/models --count --select=E9,F63,F7,F82 --show-source --statistics src
          # exit-zero treats all errors as warnings. The GitHub editor is 127 chars wide
          . env/bin/activate; env/bin/flake8 --extend-exclude src/pyfx/dispatch/oanda/models --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics src
      - name: Test with pytest
        run: |
          . env/bin/activate; env/bin/pytest test
